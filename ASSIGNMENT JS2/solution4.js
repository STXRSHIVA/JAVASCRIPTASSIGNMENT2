// Example usage
let personAge = 16;

// Ternary operator to check voting eligibility
let eligibilityMessage = personAge < 18 ? "You cannot vote" : "You can vote";

// Display the result
console.log(eligibilityMessage);
